import 'package:flutter/material.dart';

class StHomeAppBar extends StatelessWidget {
  const StHomeAppBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 40,
      color: Colors.amber,
    );
  }
}
